# BUILD INSTRUCTIONS #

Packet Peeper depends on BreakPad and HexFiend frameworks, to download and build them run `fetch_and_build_dependencies.sh`

To build a dmg file for release run `make_dmg.sh`. This will invoke fetch_and_build_dependencies.sh. The dmg file will be placed into build/Release.
